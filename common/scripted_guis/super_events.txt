scripted_gui = {
	super_event = {
		context_type = player_context
		window_name = "super_event_window"
		visible = {
			has_variable = SE_var
			is_ai = no
		}
		properties = {
			SE_image = {
				image = "[GetSuperEventImage]"
			}
		}
		effects = {
			option_click = {
				clear_variable = SE_var
			}
		}
	}
}