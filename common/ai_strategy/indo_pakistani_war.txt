RAJ_intervene_in_KSH = {
    allowed = {
        original_tag = RAJ
    }
    enable = {
		has_war_together_with = KSH
    }
    abort = {
		OR = {
			has_country_flag = RAJ_refused_to_intervene_KSH
			AND = {
				has_country_flag = RAJ_agreed_to_intervene_KSH
				KSH = {
					exists = no
				}
			}
		}
    }
    ai_strategy = {
        type = invade
        id = PAK
        value = 200
    }
}