add_namespace = BUL

news_event = {
	id = BUL.1
	title = BUL.1.title
	desc = BUL.1.desc
	picture = BUL_death_of_boris_iii

	major = yes

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = BUL.1.a
	}
}
country_event = {
	id = BUL.2
	title = BUL.2.title
	desc = BUL.2.desc

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = BUL.2.a

		hidden_effect = {
			BUL = {
				retire_character = BUL_boris_iii
				promote_character = BUL_simeon_ii
			}
			every_country = {
				limit = { is_ai = no }
				news_event = {
					id = BUL.1
					days = 1
				}
			}
		}
	}
}