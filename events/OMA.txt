add_namespace = OMA

news_event = {
	id = OMA.1
	title = OMA.1.title
	desc = OMA.1.desc
	picture = OMA_jebel_akhdar_war

	major = yes

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = OMA.1.a
	}
}
country_event = {
	id = OMA.2
	title = OMA.2.title
	desc = OMA.2.desc

	is_triggered_only = yes
	fire_only_once = yes

	immediate = {
		OMA = {
			release = OMI
		}
		OMI = {
			declare_war_on = {
				target = OMA
				type = independence_wargoal
			}
		}
	}

	option = {
		name = OMA.2.a
		hidden_effect = {
			every_country = {
				limit = { is_ai = no }
				news_event = {
					id = OMA.1
					days = 1
				}
			}
		}
	}
}