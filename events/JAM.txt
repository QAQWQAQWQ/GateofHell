add_namespace = JAM

news_event = {
	id = JAM.1
	title = JAM.1.title
	desc = JAM.1.desc
	picture = JAM_alexander_bustamante

	major = yes

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = JAM.1.a
	}
}
country_event = {
	id = JAM.2
	title = JAM.2.title
	desc = JAM.2.desc

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = JAM.2.a

		hidden_effect = {
			ENG = {
				every_owned_state = {
					limit = {state = 689}
					JAM = {transfer_state = PREV}
				}
			}
			every_country = {
				limit = { is_ai = no }
				news_event = {
					id = JAM.1
					days = 1
				}
			}
		}
	}
}