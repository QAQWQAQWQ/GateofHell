add_namespace = MLD

news_event = {
	id = MLD.1
	title = MLD.1.title
	desc = MLD.1.desc
	picture = MLD_abolition_of_monarchy

	major = yes

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = MLD.1.a
	}
}

country_event = {
	id = MLD.2
	title = MLD.2.title
	desc = MLD.2.desc

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = MLD.2.a
		hidden_effect = {
			MLD = {
				retire_character = MLD_abdul_majeed_didi
				set_politics = {
					ruling_party = progressive
				}
			}
			every_country = {
				limit = { is_ai = no }
				news_event = {
					id = MLD.1
					days = 1
				}
			}	
		}
	}
}