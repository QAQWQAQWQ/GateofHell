add_namespace = FER

news_event = {
	id = FER.1
	title = FER.1.title
	desc = FER.1.desc
	picture = FER_kerensky

	major = yes
	
	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = FER.1.a
	}
}
country_event = {
	id = FER.2
	title = FER.2.title
	desc = FER.2.desc

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = FER.2.a

		hidden_effect = {
			Fareast_Kerensky = yes

			every_country = {
				limit = { is_ai = no }
				news_event = {
					id = FER.1
					days = 1
				}
			}
		}
	}
}