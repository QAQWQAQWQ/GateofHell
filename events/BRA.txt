add_namespace = BRA

news_event = {
	id = BRA.1
	title = BRA.1.title
	desc = BRA.1.desc
	picture = BRA_return_of_vargas

	major = yes

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = BRA.1.a

		custom_effect_tooltip = tt_event_BRA1
	}
}
country_event = {
	id = BRA.2
	title = BRA.2.title
	desc = BRA.2.desc

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = BRA.2.a

		hidden_effect = {
			every_country = {
				limit = { is_ai = no }
				news_event = {
					id = BRA.1
					days = 1
				}
			}
			BRA = {
				country_event = {
					id = BRA.3
					days = 120
				}
			}
		}
	}
}
country_event = {
	id = BRA.3
	title = BRA.3.title
	desc = BRA.3.desc

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = BRA.3.a
		
		BRA = {
			retire_character = BRA_eurico_gaspar_dutra
		
			set_politics = {
				ruling_party = paternal
			}
		}
	}
}
news_event = {
	id = BRA.4
	title = BRA.4.title
	desc = BRA.4.desc
	picture = BRA_world_cup

	major = yes

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = BRA.4.a
		trigger = {
			NOT = {TAG = GER TAG = URG}
		}
	}
	option = {
		name = BRA.4.b
		trigger = {
			tag = GER
		}
	}
	option = {
		name = BRA.4.c
		trigger = {
			tag = URG
		}
	}
}